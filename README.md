# number-guessing-game-c
A simple number guessing game written in C where the program generates a random number between 1 and 100 and the user tries to guess it. The game provides hints (higher or lower) and counts the total number of attempts.
